/* Terence Washington
 * WPF Section 01
 * Grade Letter Calculator
 * 8-19-2014
 */

// set the prompt to input a grade.
var sPercent = Number(prompt("Enter your grade", 90));
var sGrade;

// calculating the first range of grade
if (sPercent == 100 || sPercent >= 90) {
	alert("You have a " + sPercent + "%, which means you have earned a(n) " + sGrade + " in the class!");
	console.log("You have a");
}