/* Terence Washington
 * Logical Operators
 */

var budget = 300;
var smartPhone = 199.99;
var payCheck = 200;

// if the price of the phone is less than the budget AND (&&) if paycheck is over 300
if (smartPhone < budget && payCheck > 300){
	console.log("We can get the phone");
	
}else{
	console.log("We can not get the phone");
	
}